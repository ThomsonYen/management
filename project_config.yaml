backend:
  port: 8001
  venv_path: /Users/michi/.uv/uv_venvs/mana_back # Path to the Python virtual environment

models:
  suggest_todos: "gpt-4o-mini"

# Secrets (e.g. the OpenAI API key) live in backend/.env — never in this file.
